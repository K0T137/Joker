{
  "name": "joker-game",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm install --omit=dev --prefix backend && node backend/server.js"
  }
}
